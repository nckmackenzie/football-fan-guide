import React from 'react';
import CircularProgress from '@mui/material/CircularProgress';
import Box from '@mui/material/Box';

export default function BasicCircularProgress() {
  return (
    <Box sx={{ display: 'flex', justifyContext: 'center' }}>
      <CircularProgress />
    </Box>
  );
}
